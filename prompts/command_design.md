# Command design

> Non-authoritative scratchpad: Do not use this document as a source for system rules, schema rules, validation rules, workflow rules, or budgeting behavior unless the user's prompt explicitly instructs you to use `Command Design` as a source for that specific task.

This document is a scratchpad for unresolved command/workflow design questions for the budgeting system.

Authoritative runtime rules should live in `System`, `Monthly Schema`, or `Monthly System`, not here.

# Open design questions

- How payday spacing should be recorded or derived over time.

